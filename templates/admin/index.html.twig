{% extends 'base2.html.twig' %}

{% block title %}Lista de postulantes{% endblock %}

{% block body %}
    <h1>Bolsa de trabajo</h1>

    <table class="table table-striped">
        <thead>
        <tr>
            <th>Nombre y Apellido</th>
            <th>Email</th>
            <th>Perfil</th>
            <th>Nivel de estudio</th>
            <th>actiones</th>
        </tr>
        </thead>
        <tbody>
        {% for user in pagination %}
            <tr>
                <td>{{ user.apellido }} {{ user.nombre }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.perfil}}</td>
                <td>{{ user.estudios }}</td>
{#                <td>#}
{#                    {% for role in user.roles %}#}
{#                        {{ role }}<br>#}
{#                    {% endfor %}#}
{#                </td>#}
                <td>
                    <a href="{{ path('admin_show', {'id': user.id}) }}" class="btn btn-primary">Ver</a>
{#                    <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-primary">edit</a>#}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

{#    <a href="{{ path('user_new') }}" class="btn btn-primary">Create new</a>#}
{% endblock %}